# sinequa-portal-api
API interna do Portal Sinequa (serve os blocos de dados). Sem segredos: token e
conexão vêm por variável de ambiente. Rodar: `PORTAL_DB=pg PORTAL_TOKEN=<token> node portal.js` (porta 8092).
